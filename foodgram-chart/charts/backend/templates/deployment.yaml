apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "backend.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
      - name: wait-for-db
        image: postgres:15-alpine
        command:
        - sh
        - -c
        - |
          until pg_isready -h {{ .Values.database.host }} -p {{ .Values.database.port }} -U {{ .Values.database.username }}; do
            echo "Waiting for database..."
            sleep 2
          done
        env:
        - name: PGPASSWORD
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: postgres-credentials
              key: password
          {{- else }}
          value: {{ .Values.secrets.postgresPassword | quote }}
          {{- end }}
      {{- if .Values.collectstatic.enabled }}
      - name: collectstatic
        image: {{ include "backend.image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command: 
        - sh
        - -c
        - python manage.py collectstatic --noinput
        envFrom:
        - configMapRef:
            name: {{ include "backend.fullname" . }}-config
        env:
        - name: POSTGRES_PASSWORD
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: postgres-credentials
              key: password
          {{- else }}
          value: {{ .Values.secrets.postgresPassword | quote }}
          {{- end }}
        - name: SECRET_KEY
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: django-credentials
              key: secret-key
          {{- else }}
          value: {{ .Values.secrets.djangoSecretKey | quote }}
          {{- end }}
        - name: DEBUG
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: django-credentials
              key: debug
          {{- else }}
          value: {{ .Values.secrets.debug | quote }}
          {{- end }}
        - name: ALLOWED_HOSTS
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: django-credentials
              key: allowed-hosts
          {{- else }}
          value: {{ .Values.secrets.allowedHosts | quote }}
          {{- end }}
        {{- if .Values.redis.enabled }}
        - name: REDIS_PASSWORD
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: redis-credentials
              key: password
          {{- else }}
          value: {{ .Values.redis.password | quote }}
          {{- end }}
        {{- end }}
        {{- if .Values.rabbitmq.enabled }}
        - name: RABBITMQ_USERNAME
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: rabbitmq-credentials
              key: username
          {{- else }}
          value: {{ .Values.rabbitmq.username | quote }}
          {{- end }}
        - name: RABBITMQ_PASSWORD
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: rabbitmq-credentials
              key: password
          {{- else }}
          value: {{ .Values.rabbitmq.password | quote }}
          {{- end }}
        {{- end }}
        {{- if .Values.celery.enabled }}
        - name: CELERY_BROKER_URL
          value: {{ .Values.celery.brokerUrl | quote }}
        - name: CELERY_RESULT_BACKEND
          value: {{ .Values.celery.resultBackend | quote }}
        {{- end }}
        volumeMounts:
        - name: static-volume
          mountPath: /app/static
        {{- if .Values.persistence.media.enabled }}
        - name: media-volume
          mountPath: /app/media
        {{- end }}
      {{- end }}
      containers:
      - name: backend
        image: {{ include "backend.image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.service.targetPort }}
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: {{ include "backend.fullname" . }}-config
        env:
        - name: POSTGRES_PASSWORD
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: postgres-credentials
              key: password
          {{- else }}
          value: {{ .Values.secrets.postgresPassword | quote }}
          {{- end }}
        - name: SECRET_KEY
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: django-credentials
              key: secret-key
          {{- else }}
          value: {{ .Values.secrets.djangoSecretKey | quote }}
          {{- end }}
        - name: DEBUG
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: django-credentials
              key: debug
          {{- else }}
          value: {{ .Values.secrets.debug | quote }}
          {{- end }}
        - name: ALLOWED_HOSTS
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: django-credentials
              key: allowed-hosts
          {{- else }}
          value: {{ .Values.secrets.allowedHosts | quote }}
          {{- end }}
        {{- if .Values.redis.enabled }}
        - name: REDIS_PASSWORD
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: redis-credentials
              key: password
          {{- else }}
          value: {{ .Values.redis.password | quote }}
          {{- end }}
        {{- end }}
        {{- if .Values.rabbitmq.enabled }}
        - name: RABBITMQ_USERNAME
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: rabbitmq-credentials
              key: username
          {{- else }}
          value: {{ .Values.rabbitmq.username | quote }}
          {{- end }}
        - name: RABBITMQ_PASSWORD
          {{- if .Values.externalSecrets.enabled }}
          valueFrom:
            secretKeyRef:
              name: rabbitmq-credentials
              key: password
          {{- else }}
          value: {{ .Values.rabbitmq.password | quote }}
          {{- end }}
        {{- end }}
        {{- if .Values.celery.enabled }}
        - name: CELERY_BROKER_URL
          value: {{ .Values.celery.brokerUrl | quote }}
        - name: CELERY_RESULT_BACKEND
          value: {{ .Values.celery.resultBackend | quote }}
        {{- end }}
        volumeMounts:
        {{- if .Values.persistence.static.enabled }}
        - name: static-volume
          mountPath: /app/static
        {{- end }}
        {{- if .Values.persistence.media.enabled }}
        - name: media-volume
          mountPath: /app/media
        {{- end }}
        {{- with .Values.resources }}
        resources:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      volumes:
      {{- if .Values.persistence.static.enabled }}
      - name: static-volume
        persistentVolumeClaim:
          claimName: {{ include "backend.fullname" . }}-static
      {{- end }}
      {{- if .Values.persistence.media.enabled }}
      - name: media-volume
        persistentVolumeClaim:
          claimName: {{ include "backend.fullname" . }}-media
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
