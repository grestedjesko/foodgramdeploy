---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: foodgram
  labels:
    app: frontend
    tier: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
    tier: frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
